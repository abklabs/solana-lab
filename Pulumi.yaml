name: svmkit-wireguard-playground
description: Playground for implementing SPEs over wireguard
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
